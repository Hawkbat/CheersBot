{
  "name": "cheers-bot",
  "version": "1.1.0",
  "description": "Heccin cheers bot!",
  "main": "index.js",
  "dependencies": {
    "open-cli": "^6.0.1",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && cd client && npm run build && cd ../server && npm run build && cd ../",
    "build:local": "npm run build && open-cli https://cheers-dev.hawk.bar/"
  },
  "author": "Hawkbat",
  "license": "UNLICENSED"
}